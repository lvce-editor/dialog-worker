# dialog-worker

Dialog worker for web and Electron confirmation prompts and renderer-hosted dialog views.

Renderer-hosted views expose generic `Dialog.show` and `Dialog.showWarning` commands. A dialog accepts a title, message, optional `info`, `warning`, or `error` type, and optional labels for the confirm and close controls.
